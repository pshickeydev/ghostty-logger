# Append to ~/.config/ghostty/config

# "Start Logger": type `ghostty-logger` into the focused terminal.
# Inside a logged session: `exit` or Ctrl-D ends logging.
keybind = ctrl+shift+l=text:ghostty-logger\n
